pts = find(labels==0);
zeros = [X(pts), Y(pts), Z(pts)];

pts = find(labels==1);
ones = [X(pts), Y(pts), Z(pts)];

pts = find(labels==2);
twos = [X(pts), Y(pts), Z(pts)];

pts = find(labels==3);
threes = [X(pts), Y(pts), Z(pts)];

pts = find(labels==4);
fours = [X(pts), Y(pts), Z(pts)];

pts = find(labels==5);
fives = [X(pts), Y(pts), Z(pts)];

pts = find(labels==6);
sixes = [X(pts), Y(pts), Z(pts)];

pts = find(labels==7);
sevens = [X(pts), Y(pts), Z(pts)];

pts = find(labels==8);
eights = [X(pts), Y(pts), Z(pts)];

pts = find(labels==9);
nines = [X(pts), Y(pts), Z(pts)];
